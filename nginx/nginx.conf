upstream todo {
    server web:8000;
}
server {
    listen 80;
    server_name 62.84.122.78;
    location / {
        proxy_pass http://todo;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
}
